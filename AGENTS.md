# Technologies
- astro ^5.17.x
- pnpm

# Dev environment tips
- Use `pnpm run dev>` to start the development server.
- Use `pnpm run build>` to build the code.
- Install dependencies with `pnpm install` or `pnpm i`.
 
# Testing
- Always add unit testing for utility functions.
